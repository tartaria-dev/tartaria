[Unit]
Description=Create subsystem overlay directories
After=var.mount
BindsTo=var.mount

[Service]
Type=oneshot
ExecStart=/usr/bin/bash -c "mkdir -p /var/lib/subsystem/{,rootfs,.rootfs-work}"

[Install]
WantedBy=multi-user.target default.target