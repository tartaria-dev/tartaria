#!/bin/bash

set -e

for PKG in /packages/*; do
    mkdir -p /tmp/pkg-extract
    tar -xf "$PKG" -C /tmp/pkg-extract

    rsync -av --exclude='.PKGINFO' --exclude='.BUILDINFO' \
        --exclude='.MTREE' --exclude='.INSTALL' \
        /tmp/pkg-extract/ /

    rm -rf /tmp/pkg-extract
done